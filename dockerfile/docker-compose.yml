version: "3.9"
services:
  back:
    image: shooting_gin
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ../workdir:/home/workdir
    working_dir: /home/workdir
    container_name: shooting_gin
    command: sh -c "tail -f /dev/null"